from setuptools import setup

setup(
    name="conv_layer",
    version='0.1',
    description='Convolution layer (forward only)',
    author='Zehua Chen',
    author_email='peterchen06242000@outlook.com',
    packages=['conv_layer'])
